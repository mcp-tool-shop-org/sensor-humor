# Source and dev files
src/
tests/
scripts/
voice-demos/
site/
.github/
.claude/

# Config
tsconfig.json
.env
.env.example
.gitignore
.polyglot-cache.json

# Docs
ROADMAP.md

# NOTE: package.json "files" (dist + CHANGELOG + SECURITY) is the authoritative allowlist.
# npm ALWAYS force-includes the root README and any root README.<lang>.md translations regardless
# of this file or "files" — they cannot be excluded here, so the language READMEs do ship (small,
# GitHub-facing; accepted). Dev-only src/scorecard is kept out of dist via tsconfig.build.json.
