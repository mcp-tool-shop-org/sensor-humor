node_modules/
dist/
.env
*.tsbuildinfo
site/.astro/
site/dist/
site/node_modules/
.polyglot-cache.json

# Local voice demo artifacts (large .wav binaries; regenerate with scripts/voice-demo.py)
voice-demos/

# Swarm runner scratch — machine-local manifests (carry an absolute local_path + save-point tags,
# not portable). The durable study-swarm EVIDENCE (evidence/, the dispatch) IS committed as provenance;
# only the runner state files are ignored.
swarms/**/manifest.json
swarms/**/manifest.*.json
